{
    "name": "vzgcoders/discordphp-bridge-telegram",
    "description": "The Telegram connector for DiscordPHP-Bridge: a two-way chat bridge with edits and media, Components v2 panels, and one command catalogue shared with every other network the bot is on.",
    "keywords": [
        "discord",
        "telegram",
        "chat",
        "relay",
        "bridge",
        "bot",
        "reactphp",
        "async",
        "connector"
    ],
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Valithor Obsidion",
            "email": "valithor@valgorithms.com"
        }
    ],
    "require": {
        "php": "^8.4",
        "react/event-loop": "^1.5",
        "react/promise": "^3.2",
        "vzgcoders/discordphp-bridge": "@dev",
        "vzgcoders/telegramphp": "@dev"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.64",
        "phpunit/phpunit": "^11.0 || ^10.5"
    },
    "repositories": [
        {
            "type": "path",
            "url": "../DiscordPHP-Bridge",
            "options": {
                "symlink": true
            }
        },
        {
            "type": "path",
            "url": "../TelegramPHP",
            "options": {
                "symlink": true
            }
        }
    ],
    "autoload": {
        "psr-4": {
            "Bridge\\Telegram\\": "src/Bridge/Telegram/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Bridge\\Telegram\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "cs": "php-cs-fixer fix",
        "test": "phpunit"
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "sort-packages": true
    }
}
